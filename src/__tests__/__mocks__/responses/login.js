import { accessToken } from "../items/accessToken";

/**
 * The answer to /auth/login call
 */
export const item = {
	"roles": ["ROLE_CUSTOMER"],
	"access_token": accessToken,
	"expiry_at": "1673267808000"
};